# Required by AGP 8 when any AndroidX dependency is in the graph.
# We pull androidx.lifecycle:lifecycle-process for app-lifecycle hooks.
android.useAndroidX=true

# AGP 8 best practice — namespaced resources, smaller R class.
android.nonTransitiveRClass=true
